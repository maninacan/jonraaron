// import { render } from '@testing-library/react';

import NavDropdown from './nav-dropdown';

describe('NavDropdown', () => {
  it.todo(
    'should render successfully' /*, () => {
    const { baseElement } = render(<NavDropdown />);
    expect(baseElement).toBeTruthy();
  }*/
  );
});
